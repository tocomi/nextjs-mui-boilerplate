import { Typography } from '@mui/material'
import type { NextPage } from 'next'

const Artifacts: NextPage = () => {
  return <Typography variant="h1">ARTIFACTS</Typography>
}

export default Artifacts
